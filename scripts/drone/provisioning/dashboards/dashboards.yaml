apiVersion: 1

providers:
 - name: 'Tests'
   folder: 'tests'
   folderUid: ''
   type: file
   allowUiUpdates: false
   updateIntervalSeconds: 60
   options:
     path: /drone/src/devenv/scripts/drone/provisioning/dashboards
